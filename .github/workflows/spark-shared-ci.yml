name: Spark shared CI

on:
  push:
    branches:
      - main
      - master
      - "codex/**"
      - "reconcile/**"
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  security-events: write

jobs:
  conformance:
    uses: vibeforge1111/spark-cli/.github/workflows/reusable-conformance.yml@main
    with:
      python-version: "3.11"
      install-command: "python -m pip install -e . pytest"
      test-command: "python -m pytest -q"
      conformance-command: "python -c \"from spark_character import score_persona; score_persona('I can help.')\""

  scanners:
    uses: vibeforge1111/spark-cli/.github/workflows/reusable-scanners.yml@main
